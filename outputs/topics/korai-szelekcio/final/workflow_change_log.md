# Workflow change log

The workflow steps (docs/workflow.md, config/system_config.json) were stable across rounds; all applied changes were agent-spec directives (see agent_change_log.md). Each round's effective workflow is snapshotted in round_XX/system_state/system_config.json.
